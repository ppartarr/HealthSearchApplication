#TODO fix or remove

#from eHealth.models import UserProfile
#from registration.signals import user_registered
#from eHealth.forms import UserProfileFormTest


#def user_registered_callback(sender, user, request, **kwargs):
 #   form = UserProfileFormTest(request.POST)
  #  data = UserProfile(user = user)
   # data.dateOfBirth = form.data["dateOfBirth"]
    #data.gender = form.data["gender"]
    #data.picture = form.data["picture"]
    #data.save()

#user_registered.connect(user_registered_callback)